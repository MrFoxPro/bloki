type EditType = 'drag' | 'resize' | 'select' | 'content';

enum Instrument {
   Cursor,
   Pencil,
   Pen,
   Marker,
   Lastik,
   Circle,
   Triangle,
   Rect
}

export {
   EditType,
   Instrument
};