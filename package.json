{
	"homepage": "https://bloki.app",
	"scripts": {
		"test": "pnpm run -F tests start",
		"dev": "pnpm run --parallel --stream -F api -F client dev",
		"ci:tg": "node ./notify-tg.js"
	},
	"workspaces": [
		"api",
		"client",
		"tests",
		"./misc/tg-notifier"
	],
	"devDependencies": {
		"cross-env": "^7.0.3"
	}
}