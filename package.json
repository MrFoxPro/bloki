{
   "homepage": "https://bloki.app",
   "scripts": {
      "test": "pnpm run -F tests start",
      "dev": "pnpm run --parallel --stream -F web -F api dev",
      "build": "pnpm run --parallel -F web build"
   },
   "workspaces": [
      "api",
      "web",
      "tests",
      "./misc/tg"
   ],
   "pnpm": {
      "overrides": {
         "babel-preset-solid": "1.5.7"
      }
   }
}
